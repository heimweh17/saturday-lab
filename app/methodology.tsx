import type {Manifest,Report} from './types';
import {asset} from '@/lib/paths';
import {Table,TableHeader,TableBody,TableHead,TableRow,TableCell} from '@/components/ui/table';
import {dateLabel} from './ui';
import production from '@/lib/production-config.json';
export function Methodology({manifest,report}:{manifest:Manifest;report:Report}){const c=report.config;return <><div className="section-heading"><div><h1>A model you can question.</h1><p>The equations, evidence and limits behind Saturday Lab.</p></div><a className="text-link" href={asset('/data/model-predictions.csv')} download>Download model predictions</a></div><div className="method-layout"><aside className="method-index"><a href="#rating">The model</a><a href="#validation">Historical testing</a><a href="#definitions">Metric dictionary</a><a href="#coverage">Data coverage</a><a href="#limits">What is missing</a><a href="#api">Use the data</a></aside><article className="method-article">
<section id="rating"><h2>Scoring, efficiency and preseason evidence</h2><p>The redesigned model learns one win probability from pregame scoring strength, opponent-adjusted EPA and special teams, signed-class recruiting talent, venue and rest. The current specification is selected using earlier seasons, never a desired school's rank or ESPN's ranking as a target.</p><div className="formula">Unit performance = league mean + offense - opponent defense<br/>P(win) = logistic(sum of coefficient x pregame feature)</div><p>Each unit rating minimizes weighted squared errors plus a ridge penalty toward a shrunken preseason estimate. Candidate scoring designs use raw margins, clip margins at 28 points, or transform them as 28 x tanh(margin / 28). Compression preserves the game total while changing the fitted margin; it is not play-level garbage-time filtering.</p><p>Scoring candidates test 40% and 65% carryover, one or three prior seasons (weights 1, 0.5, 0.25), and rating penalties 1 and 4. Weekly decay is .97; scoring home advantage is three points. The selected current scoring component uses penalty {c.scoring.shrinkage}, decay {c.scoring.decay}, and a league-mean prior of 28 with weight 20.</p><p>EPA per play, passing EPA, rushing EPA, special-teams EPA per game and yards per play receive separate joint offense/opponent-defense fits. These unit fits use penalty 2, 65% carryover, .97 decay and no preset unit home adjustment. Missing pairs are omitted. EPA is supplied by the public cfbfastR pipeline, not ESPN's proprietary EPA. Its historical estimation may reflect later data and remains a retrospective limitation.</p><p>Recruiting inputs are a four-class talent composite and blue-chip share, not a current transfer-adjusted roster. An early-season talent interaction decays as exp(-min(games played)/4). Missing talent uses the current FBS mean plus a coverage indicator. Same-season returning-production files were excluded because their roster availability is not established before kickoff.</p><p>The current model selects <strong>{production.selected.bundle.replaceAll('_',' ')}</strong>, regression penalty {production.selected.ridge}, fitted through {production.trainingThrough}. Training-only feature scaling, antisymmetric inputs and no intercept ensure that swapping teams swaps probabilities. Matchup contributions explain the complete log odds. Hypothetical matchups use equal rest.</p><h3>Rank the field on equal terms</h3><div className="formula">Neutral win index(A) = average P(A beats B)<br/>over every other same-season FBS team B</div><p>Rankings use neutral venues and equal rest. Scoring offense, defense and points remain one interpretable component; a points favorite can differ from the complete model's favorite. No school-specific ranking adjustments are applied.</p></section>
<section id="validation"><h2>The calendar is the boundary</h2><p>The frozen research grid contains 288 configurations: 24 rating designs x six feature bundles x two regression penalties. For each target year, select minimum pooled log loss over its preceding three years. Each of those inner years is predicted using coefficients fitted only on earlier seasons. Refit the selected specification through targetYear-1.</p><p>2018 warms up ratings; supervised training begins in 2019. Annual model selection begins in 2023. The 2018-2022 archive retains exploratory scoring forecasts. Each prediction week is frozen before its first kickoff. Later results never enter a team's earlier-week ratings.</p><p>These are retrospective experiments on corrected historical data, not pristine prospective tests. Model development has already inspected these years, and upstream EPA estimation has its own temporal limitations. Model audit publishes all candidates, comparisons with two older models, early-season and cross-conference slices, calibration, a paired week bootstrap and every prediction. Aggregate improvement does not establish superiority in every subgroup.</p><p>ESPN FPI is an external reference. Historical FPI-based picks use only timestamps before the prediction cutoff, plus our fixed three-point home adjustment; they are not ESPN's official game probabilities. Current ranking agreement uses separately dated snapshots and does not drive model selection.</p></section>
<section id="definitions"><h2>Read the numbers precisely</h2><dl className="dictionary"><div><dt>Brier score</dt><dd>Mean (probability − outcome)². Lower is better; predicting 50% always gives 0.25.</dd></div><div><dt>Log loss</dt><dd>−mean[y log(p) + (1−y) log(1−p)]. Penalizes confident errors and selects the configuration.</dd></div><div><dt>Scoring power</dt><dd>Adjusted offense + defense, in points per game. A component, not the complete model ranking.</dd></div><div><dt>Schedule strength</dt><dd>Mean current scoring power of completed FBS opponents. This display is diagnostic, not a separate input in the selected current model.</dd></div><div><dt>Expected remaining wins</dt><dd>Sum of probabilities across modeled remaining games. Not the number of picks above 50%; unmodeled opponents are excluded.</dd></div><div><dt>Remaining-win distribution</dt><dd>Exact Poisson-binomial calculation assuming independent outcomes and fixed current strength. A scenario, not a calibrated season-record forecast.</dd></div><div><dt>Team data rates</dt><dd>Summed numerators divided by summed denominators. Other summaries are per available game. FBS reference values average team aggregates.</dd></div></dl></section>
<section id="coverage"><h2>Real games and explicit coverage</h2><p><a href="https://github.com/sportsdataverse/sportsdataverse-data" target="_blank" rel="noreferrer">SportsDataverse releases</a> provide ESPN-derived historical schedules, membership and box scores. The <a href="https://site.api.espn.com/apis/site/v2/sports/football/college-football/teams/194/schedule?season=2026" target="_blank" rel="noreferrer">public ESPN team-schedule endpoint</a> provides remaining fixtures. The research extension also uses public cfbfastR EPA game logs and four-class recruiting composites. Schedules and box-score deliveries share ESPN upstream; they are not independent corroboration. All research source URLs and hashes appear in the model report.</p><p>FBS-vs-FCS games appear in records and raw statistics, but not rating fits or model accuracy. Remaining FCS games have no fabricated probability. Unannounced postseason matchups are not invented.</p><p>Snapshot built {dateLabel(manifest.generatedAt)}. The site does not claim live updates; result cutoffs and schedule retrieval dates appear separately.</p><Table><TableHeader><TableRow>{['Season','FBS teams','All games','Model games','Last result'].map(x=><TableHead key={x}>{x}</TableHead>)}</TableRow></TableHeader><TableBody>{manifest.audit.map(a=><TableRow key={a.season}><TableCell>{a.season}</TableCell><TableCell>{a.teams}</TableCell><TableCell>{a.games}</TableCell><TableCell>{a.modelGames}</TableCell><TableCell>{dateLabel(a.lastGame)}</TableCell></TableRow>)}</TableBody></Table></section>
<section id="limits"><h2>What the data cannot tell us</h2><p>The model has no verified injury, depth-chart, transfer, quarterback-change, coaching-change, weather or betting-line inputs. Signed-class recruiting is an imperfect roster proxy. The sourced EPA is not ESPN's proprietary EPA, and this model does not claim verified garbage-time filtering.</p><p>Small samples, newly promoted teams and sparse cross-conference games remain uncertain. Missing EPA observations are omitted, not filled with zero. The new model improves aggregate probability loss versus v3, but some late-season slices and the original scoring benchmark still perform better.</p><p>Remaining-game forecasts hold current strength fixed. The exact win-count distribution assumes independent games and does not model shared injury risk or future strength changes. Scoring components include defensive and special-teams points.</p></section>
<section id="api"><h2>Reproduce it or build on it</h2><p><a href="https://github.com/heimweh17/saturday-lab" target="_blank" rel="noreferrer">Source</a> · <a href={asset('/data/manifest.json')} target="_blank">Manifest</a> · <a href={asset('/data/model.json')} target="_blank">Model report</a> · <a href={asset('/data/fixtures.json')} target="_blank">Schedules</a> · <a href={asset('/data/model-predictions.csv')} download>Predictions</a></p><div className="formula">GET /saturday-lab/data/2026.json<br/>GET /saturday-lab/data/box-2026.json<br/>GET /saturday-lab/data/model.json</div><p>GitHub Pages serves versioned JSON datasets. Matchups run in the browser using the same formula. Optional Next.js API routes remain in the source for server hosting. ESPN IDs identify teams; snapshot 99 is latest and other week keys are before-week states.</p><p>The repository documents ingestion, regeneration, the search protocol and tests. Team marks belong to their respective owners.</p></section></article></div></>}

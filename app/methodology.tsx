import type {Manifest,Report} from './types';
import {asset} from '@/lib/paths';
import {Table,TableHeader,TableBody,TableHead,TableRow,TableCell} from '@/components/ui/table';
import {dateLabel} from './ui';
import production from '@/lib/production-config.json';
const pctValue=(v:number)=>`${(v*100).toFixed(0)}%`;
export function Methodology({manifest,report}:{manifest:Manifest;report:Report}){const c=report.config;return <><div className="section-heading"><div><h1>How predictions work.</h1><p>The equations, evidence and limits behind Saturday Lab.</p></div><a className="text-link" href={asset('/data/model-predictions.csv')} download>Download model predictions</a></div><div className="method-layout"><aside className="method-index"><a href="#rating">The model</a><a href="#validation">Historical testing</a><a href="#definitions">Metric dictionary</a><a href="#coverage">Data coverage</a><a href="#limits">What is missing</a><a href="#api">Use the data</a></aside><article className="method-article">
<section id="rating"><h2>One strength rating for each team</h2><p>Scoring, first-down creation, passing, rushing, yards per play and other efficiency measures are each adjusted for opponent quality before they can enter the model. Recruiting can inform the preseason scoring prior, but is never a separate weekly bonus. Historical validation selects which inputs to use; no school receives a manual adjustment.</p><div className="formula">S(team) = sum of nonnegative weight × opponent-adjusted component<br/>P(A wins) = logistic(S(A) − S(B) + venue + rest)</div><p>On a neutral field with equal rest, every team has one strength score. Its value does not depend on the opponent’s sample count. Game forecasts add venue and rest; hypothetical comparisons assume equal rest.</p><h3>Start with a prior, then learn from games</h3><div className="formula">Preseason scoring strength = carry × previous final strength<br/>+ recruiting weight × (standardized recruiting proxy)</div><p>The recruiting adjustment is split equally between the offense and defense priors. Game updates jointly estimate offense and opponent defense by minimizing weighted squared prediction errors plus a penalty toward those priors. Missing recruiting data use the season’s FBS mean, producing zero centered recruiting adjustment.</p><p>Current configuration: {pctValue(production.selected.ratingConfig.carry)} carryover, {production.selected.ratingConfig.talentMode.replaceAll('_',' ')} recruiting-prior weight {production.selected.ratingConfig.talentPrior}, rating penalty {c.scoring.shrinkage}, {production.selected.ratingConfig.method} margins and {production.selected.ratingConfig.weight} game weights. Scoring home advantage is three points; weekly observation decay is .97. Efficiency units use half the scoring penalty, the same carryover and no preset home adjustment.</p><p>The current probability model selects the <strong>{production.selected.bundle.replaceAll('_',' ')}</strong> bundle, fitted through {production.trainingThrough}, with regression penalty {production.selected.ridge}. The fitted 2026 model gives positive weight to scoring strength, first-down creation, rushing EPA, yards per play and observed passer history; unused candidate inputs receive zero. Strength and venue coefficients are nonnegative; rest is unconstrained.</p><h3>From win probability to expected points</h3><p>The projected-score layer does not refit or alter v6. A positive robust mapping converts the complete model’s probability logit into an expected margin. A separate historical calibration of the opponent-adjusted scoring total estimates pace; the two team scores are then (total ± margin) / 2. This guarantees that the expected-score leader agrees with the published probability leader. Decimal scores are distribution averages, not claims about an exact final score.</p><p>The method was selected on rolling 2023–2024 results, then checked on the untouched 2025 season. In that final test, margin MAE improved from 12.36 to 12.18 points, total MAE from 13.19 to 12.94 and team-score MAE from 9.02 to 8.95. The 2023 team-score change was a 0.04-point regression within the predeclared 0.05 guard; 2024 and 2025 improved.</p><h3>Observed passer history</h3><p>The latest completed game identifies observed passers and their attempt shares. Their previous-season FBS-vs-FBS passing records follow stable athlete IDs, including across schools. No current or future game outcome enters this historical efficiency term.</p><div className="formula">Prior passer efficiency = (yards + 20 × TD − 45 × INT + 100 × league AYA) / (attempts + 100) − league AYA</div><p>League AYA is from the previous season. Player values are clipped to ±5, then averaged by latest-game attempt share. Before any current-season observation, the context is neutral; a passer without prior records also has neutral efficiency. It is raw, shrunken passing efficiency, not opponent-adjusted QBR or a forecast of who will start.</p><h3>What a ranking means</h3><div className="formula">Strength index(A) = average neutral-field P(A beats B)<br/>over all other FBS teams B</div><p>This ordering matches the scalar strength ordering. It measures predicted ability, not an AP-style achievement ranking. A win can still lower predicted strength if the performance falls below expectation; a team can also improve while others improve more.</p></section>
<section id="validation"><h2>Evaluate in calendar order</h2><p>Research proceeded through seven documented stages: carryover and recruiting placement, roster-cohort alternatives, observed passer history, raw pregame play features, opponent-adjusted replication, and a durability review of preseason decay, returning production and staff continuity.</p><p>For each target season, select minimum pooled log loss across the preceding three seasons. Each inner year uses coefficients trained only on earlier years. Refit the chosen configuration through targetYear−1. 2018 warms up ratings, training starts in 2019, and annual selection starts in 2023. Each game week is frozen before its first kickoff.</p><p>The v6 promotion gate required at least a .002 pooled 2024–2025 log-loss gain over v5, no material regression in either complete season, no accuracy decline larger than half a percentage point, and at least 90% week-block bootstrap probability of lower loss. The opponent-adjusted model passed all four checks.</p><p>The later durability review tested 1,215 combinations behind a separate frozen gate. Its best rolling candidate missed the minimum log-loss improvement, the 90% bootstrap threshold and the complete-season guard, so v6 stayed online. All results remain retrospective, and 2026 is still a small partial season.</p><p>ESPN FPI is an external reference, not a training label. Historical FPI-based picks use only timestamps before the prediction cutoff, plus our fixed three-point home adjustment. They are not ESPN’s official game probabilities.</p></section>
<section id="definitions"><h2>Read the numbers precisely</h2><dl className="dictionary"><div><dt>Brier score</dt><dd>Mean (probability − outcome)². Lower is better; predicting 50% always gives 0.25.</dd></div><div><dt>Log loss</dt><dd>−mean[y log(p) + (1−y) log(1−p)]. Penalizes confident errors and selects the configuration.</dd></div><div><dt>Scoring power</dt><dd>Adjusted offense + defense, in points per game. A component, not the complete model ranking.</dd></div><div><dt>Schedule strength</dt><dd>Mean current scoring power of completed FBS opponents. This display is diagnostic, not a separate input in the selected current model.</dd></div><div><dt>Expected remaining wins</dt><dd>Sum of probabilities across modeled remaining games. Not the number of picks above 50%; unmodeled opponents are excluded.</dd></div><div><dt>Remaining-win distribution</dt><dd>Exact Poisson-binomial calculation assuming independent outcomes and fixed current strength. A scenario, not a calibrated season-record forecast.</dd></div><div><dt>Team data rates</dt><dd>Summed numerators divided by summed denominators. Other summaries are per available game. FBS reference values average team aggregates.</dd></div></dl></section>
<section id="coverage"><h2>Real games and explicit coverage</h2><p><a href="https://github.com/sportsdataverse/sportsdataverse-data" target="_blank" rel="noreferrer">SportsDataverse releases</a> provide ESPN-derived historical schedules, membership and box scores. The <a href="https://site.api.espn.com/apis/site/v2/sports/football/college-football/teams/194/schedule?season=2026" target="_blank" rel="noreferrer">public ESPN team-schedule endpoint</a> provides remaining fixtures. The research extension also uses public cfbfastR EPA game logs and four-class recruiting composites. Schedules and box-score deliveries share ESPN upstream; they are not independent corroboration. All research source URLs and hashes appear in the model report.</p><p>FBS-vs-FCS games appear in records and raw statistics, but not scoring/EPA/Elo fits or model accuracy. A completed FCS game can identify the observed passers used for prior-season context. Remaining FCS games have no fabricated probability. Unannounced postseason matchups are not invented.</p><p>Snapshot built {dateLabel(manifest.generatedAt)}. The site does not claim live updates; result cutoffs and schedule retrieval dates appear separately.</p><Table><TableHeader><TableRow>{['Season','FBS teams','All games','Model games','Last result'].map(x=><TableHead key={x}>{x}</TableHead>)}</TableRow></TableHeader><TableBody>{manifest.audit.map(a=><TableRow key={a.season}><TableCell>{a.season}</TableCell><TableCell>{a.teams}</TableCell><TableCell>{a.games}</TableCell><TableCell>{a.modelGames}</TableCell><TableCell>{dateLabel(a.lastGame)}</TableCell></TableRow>)}</TableBody></Table></section>
<section id="limits"><h2>What the data cannot tell us</h2><p>The model has no verified injury, depth-chart, future-starter, coaching-change, weather or betting-line inputs. Observed passers can be linked across schools, but there is no complete transfer-adjusted roster. Signed-class recruiting is an imperfect roster proxy. The sourced EPA is not ESPN&apos;s proprietary EPA, and this model does not claim verified garbage-time filtering.</p><p>Small samples, newly promoted teams and sparse cross-conference games remain uncertain. Missing efficiency observations are omitted from that unit fit. No version wins every metric or season.</p><p>Remaining-game forecasts hold current strength fixed. The exact win-count distribution assumes independent games and does not model shared injury risk or future strength changes.</p></section>
<section id="api"><h2>Reproduce it or build on it</h2><p><a href="https://github.com/heimweh17/saturday-lab" target="_blank" rel="noreferrer">Source</a> · <a href={asset('/data/manifest.json')} target="_blank">Manifest</a> · <a href={asset('/data/model.json')} target="_blank">Model report</a> · <a href={asset('/data/fixtures.json')} target="_blank">Schedules</a> · <a href={asset('/data/model-predictions.csv')} download>Predictions</a></p><div className="formula">GET /saturday-lab/data/2026.json<br/>GET /saturday-lab/data/box-2026.json<br/>GET /saturday-lab/data/model.json</div><p>GitHub Pages serves versioned JSON datasets. Matchups run in the browser using the same formula. Optional Next.js API routes remain in the source for server hosting. ESPN IDs identify teams; snapshot 99 is latest and other week keys are before-week states.</p><p>The repository documents ingestion, regeneration, the search protocol and tests. Team marks belong to their respective owners.</p></section></article></div></>}

import type {Manifest,Report} from './types';
import {Table,TableHeader,TableBody,TableHead,TableRow,TableCell} from '@/components/ui/table';
import {dateLabel} from './ui';
export function Methodology({manifest,report}:{manifest:Manifest;report:Report}){const c=report.config;return <><div className="section-heading"><div><h1>A rating you can question<span>.</span></h1><p>The definitions, assumptions and limitations behind Saturday Lab.</p></div><a className="text-link" href="/data/predictions.csv" download>Download the complete prediction archive</a></div><div className="method-layout"><aside className="method-index"><a href="#rating">The rating</a><a href="#validation">Historical testing</a><a href="#definitions">Metric dictionary</a><a href="#coverage">Data coverage</a><a href="#limits">What is missing</a><a href="#api">Use the data</a></aside><article className="method-article"><section id="rating"><h2>Start with an opponent-adjusted scoreboard</h2><p>Scoring 35 against a good defense and scoring 35 against a weak defense are different signals. We estimate an offensive effect for the scoring team and a defensive effect for its opponent simultaneously.</p><div className="formula">Expected points = μ + offense − opponent defense + venue effect</div><p>Both teams contribute one observation per game. We minimize weighted squared prediction error plus a ridge penalty. Positive offense means points scored above average; positive defense means points prevented. These are points per game, including special-teams and defensive scoring, not per-play efficiency.</p><div className="formula">Σ w × (score − μ − O + D − H × venue)²<br/>+ λ × Σ [(O − prior O)² + (D − prior D)²]<br/>+ 20 × (μ − 28)²</div><p>Venue is +½ for the home team, −½ for the visitor and 0 on a neutral field. The selected full home-field margin is <strong>{c.scoring.home} points</strong>. Each offseason, prior offensive and defensive effects retain 65% of their previous final estimate. New FBS teams start at zero. The mean-scoring prior is 28 points with a fixed regularization weight of 20.</p><p>The selected ridge weight is <strong>{c.scoring.shrinkage}</strong> equivalent game observations and weekly decay is <strong>{c.scoring.decay}</strong>. A decay of 1 means equal weight for all completed games within the season. Coordinate descent solves the convex objective; early-season ratings are deliberately pulled toward their prior.</p><div className="formula">Power = offense + defense<br/>Expected home margin = power(home) − power(away) + H<br/>P(home wins) = 1 / (1 + exp(−margin / {c.scoring.scale}))</div><p>The score components determine the displayed point projection. The logistic scale controls probability confidence. Elo is an independent benchmark using K={c.elo.k}, home advantage {c.elo.home} Elo points and offseason carryover {c.elo.carry}. Blend weights 0, .25, .5, .75 and 1 were tested; validation selected {c.eloWeight}, so Elo does not affect the final model.</p></section>
<section id="validation"><h2>The calendar is the train/test boundary</h2><div className="timeline"><div><strong>2018–2022</strong><span>Development and warm-up</span></div><div><strong>2023</strong><span>53 candidate evaluations</span></div><div><strong>2024–2025</strong><span>Locked-parameter holdout</span></div><div><strong>2026</strong><span>Partial current-season sample</span></div></div><p>For each season, a whole week is predicted from the rating state before the first kickoff in that week. Only then are that week’s scores added. An assertion rejects any overlapping week buckets that would admit later results. Postseason weeks use separate keys because the source resets its week number.</p><p>We select Elo settings, scoring settings and then a blend using 2023 log loss. The holdout seasons are evaluated sequentially with those settings fixed. Ratings continue learning from earlier games within a holdout season, as they would in real use; hyperparameters do not. The 2018–2023 displays are exploratory because the final settings were selected using 2023.</p><p>This is a retrospective backtest using today’s corrected historical records, not proof that forecasts were published before kickoff. Historical data revisions, incomplete coverage and membership classifications may affect results. The 2020 disrupted season is in warm-up, not in the final holdout.</p></section>
<section id="definitions"><h2>Read the numbers precisely</h2><dl className="dictionary"><div><dt>Brier score</dt><dd>Mean (probability − outcome)². Lower is better. Always predicting 50% gives 0.25.</dd></div><div><dt>Log loss</dt><dd>−mean[y log(p) + (1−y) log(1−p)]. Penalizes confident mistakes more heavily. This is the selection criterion.</dd></div><div><dt>Margin MAE</dt><dd>Mean absolute difference between predicted and actual home score margin, in points. It is not the error in each team’s score.</dd></div><div><dt>Schedule strength</dt><dd>Mean of completed FBS opponents’ net offensive + defensive ratings at the selected snapshot. It is retrospective context, not a pregame feature.</dd></div><div><dt>Rank movement</dt><dd>National power-rank change from the preceding available snapshot. A positive arrow means a higher position. Ties break alphabetically.</dd></div><div><dt>Calibration</dt><dd>Home-win probabilities grouped into ten bins, compared with observed home wins. Vertical intervals use the Wilson 95% binomial formula; small samples are uncertain.</dd></div><div><dt>Box-score rates</dt><dd>Third-down and completion rates use total conversions/completions divided by total attempts, not an unweighted average of percentages. Yardage and turnovers are averages per available box score.</dd></div></dl></section>
<section id="coverage"><h2>Real games, explicit coverage</h2><p>Public <a href="https://github.com/sportsdataverse/sportsdataverse-data" target="_blank" rel="noreferrer">SportsDataverse releases</a> supply ESPN-derived schedules, team metadata and team box scores. We use same-season FBS membership and remove all-star/exhibition teams, canceled games and non-final games. FBS vs. FCS results remain in team records, but are excluded from model fitting and backtesting.</p><p>Snapshot built {dateLabel(manifest.generatedAt)}. The site serves a reproducible snapshot; it does not claim live or automatic updates. “Latest available” means latest in the source files, not a guarantee of complete real-world coverage.</p><Table><TableHeader><TableRow>{['Season','FBS teams','All games','Model games','Last result'].map(x=><TableHead key={x}>{x}</TableHead>)}</TableRow></TableHeader><TableBody>{manifest.audit.map(a=><TableRow key={a.season}><TableCell>{a.season}</TableCell><TableCell>{a.teams}</TableCell><TableCell>{a.games}</TableCell><TableCell>{a.modelGames}</TableCell><TableCell>{dateLabel(a.lastGame)}</TableCell></TableRow>)}</TableBody></Table><p className="fine-print">The manifest includes SHA-256 checksums for the raw schedules. “All games” means completed games with at least one valid FBS team.</p></section>
<section id="limits"><h2>What this model does not know</h2><p>There is no injury, player availability, recruiting, transfer, weather, betting-line or play-by-play input. Scores do not cleanly separate offensive, defensive and special-teams units. Pace and garbage time are not adjusted. No EPA, success-rate or drive efficiency is claimed without play-level data.</p><p>Preseason priors may be stale after major roster changes. FCS opponents are excluded from ratings; newly promoted teams have weak priors. The validation grid is finite and the holdout contains only two seasons. A future model revision should use a new untouched season or nested rolling validation; repeatedly optimizing against 2024–2025 would invalidate the current holdout claim.</p><p>Projected scores are continuous expectations, not valid football scoring combinations. The interface floors a negative displayed score at zero but does not change the underlying margin or probability. Historical error ranges are descriptive and are not calibrated matchup-specific prediction intervals.</p></section>
<section id="api"><h2>Reproduce, compare, build on it</h2><p><a href="/data/manifest.json" target="_blank">Data manifest</a> · <a href="/data/report.json" target="_blank">Model report</a> · <a href="/data/predictions.csv" download>Prediction CSV</a> · <a href="/api/health" target="_blank">Service health</a></p><div className="formula api-example">GET /api/matchup?season=2026&amp;week=99&amp;a=194&amp;b=333&amp;venue=neutral<br/>GET /api/ratings?season=2026&amp;week=99<br/>GET /data/2025.json</div><p>Use ESPN team IDs. Venue is a, b or neutral; week 99 selects the latest available snapshot. Other week keys represent the state before that week. Invalid seasons, weeks or identical opponents return a validation error. Responses identify the data cutoff and model version.</p><p>To refresh locally, run the Python data pipeline, then regenerate the compact service index and rebuild. Raw downloads are cached; remove only the season files you intend to refresh. The source package includes commands, tests, a model report and portfolio notes. Team marks belong to their respective owners.</p></section></article></div></>}

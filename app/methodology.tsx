import type {Manifest,Report} from './types';
import {asset} from '@/lib/paths';
import {Table,TableHeader,TableBody,TableHead,TableRow,TableCell} from '@/components/ui/table';
import {dateLabel} from './ui';
import production from '@/lib/production-config.json';
export function Methodology({manifest,report}:{manifest:Manifest;report:Report}){const c=report.config;return <><div className="section-heading"><div><h1>A model you can question.</h1><p>The equations, evidence and limits behind Saturday Lab.</p></div><a className="text-link" href={asset('/data/model-predictions.csv')} download>Download model predictions</a></div><div className="method-layout"><aside className="method-index"><a href="#rating">The model</a><a href="#validation">Historical testing</a><a href="#definitions">Metric dictionary</a><a href="#coverage">Data coverage</a><a href="#limits">What is missing</a><a href="#api">Use the data</a></aside><article className="method-article">
<section id="rating"><h2>One forecast, several kinds of evidence</h2><p>The Saturday model combines opponent-adjusted scoring with a regularized efficiency model. Visitors get one probability and one ranking. Internal feature groups and weights are selected by historical validation.</p><div className="formula">Expected points = μ + offense − opponent defense + H × venue<br/>P(win) = w × logistic(β · x) + (1 − w) × logistic(scoring margin / 8)</div><p>Scoring offense and defense jointly minimize squared score errors plus a ridge penalty toward last season’s shrunken ratings. The scoring anchor uses λ={c.scoring.shrinkage}, full home advantage {c.scoring.home} points, weekly decay {c.scoring.decay}, and 65% offseason carryover. Venue is +½ for the home scoring observation, −½ for the away observation and zero at neutral sites. μ has a prior of 28 with weight 20. Scores include special-teams and defensive points.</p><p>Ten box-score signals receive their own offensive and opponent-defensive adjustment: passing and rushing yards per attempt, third-down conversion, turnovers per play proxy, play volume, completion rate, first downs per play proxy, penalty yards per play proxy, fourth-down conversion and possession minutes. Each uses ridge weight 5, weekly decay .94 and offseason carryover .65. New teams start at zero effect; missing pairs are omitted.</p><p>Candidate inputs also include Elo, recent form, schedule strength, experience, rest and passing/rushing interactions. Logistic regression fits coefficients with an L2 penalty after training-only scaling. Antisymmetric features and no intercept guarantee complementary win probabilities when teams swap.</p><p>The current {production.trainingThrough+1} model selects <strong>{production.selected.name.replaceAll('_',' ')}</strong>, ridge λ={production.selected.ridge}, efficiency weight {production.weight}, and scoring weight {1-production.weight}. Coefficients are fitted through {production.trainingThrough}. Matchup lab contributions explain the efficiency component in log odds, not points.</p><h3>Rank the field on equal terms</h3><div className="formula">Neutral win index(A) = average P(A beats B)<br/>over every other same-season FBS team B</div><p>These comparisons use neutral venues and equal rest. Teams rank by average win probability; ties break alphabetically. Offense, defense and scoring power remain interpretable scoring-component statistics, not an additive explanation of the complete win index.</p></section>
<section id="validation"><h2>The calendar is the boundary</h2><p>There are 73 prespecified configurations: six nested input groups × three ridge penalties × four efficiency weights, plus the scoring anchor alone. For each target year, select the lowest pooled log loss over the preceding three validation years. Every validation year itself uses coefficients trained only on earlier years. Refit through the year preceding the target year, then evaluate that target year.</p><p>2018 warms up ratings; supervised fitting starts in 2019. Annual selection begins in 2023. The 2018–2022 archive retains original exploratory scoring forecasts and is not an independent test of the final model.</p><p>Each week is frozen. Scores, box statistics and opponent ratings use only games completed before its first kickoff. Prior seasons may train later years: a 2025 result can affect 2026 selection, but never a 2025 prediction.</p><p>All results are retrospective, using corrected public records. Historical seasons have already been inspected during development. These are time-separated backtests, not proof forecasts were published before kickoff. Model audit exposes candidates, benchmarks, calibration and the prediction ledger. A 3,000-resample week-cluster bootstrap describes uncertainty; especially in short samples, it is not proof of superiority.</p></section>
<section id="definitions"><h2>Read the numbers precisely</h2><dl className="dictionary"><div><dt>Brier score</dt><dd>Mean (probability − outcome)². Lower is better; predicting 50% always gives 0.25.</dd></div><div><dt>Log loss</dt><dd>−mean[y log(p) + (1−y) log(1−p)]. Penalizes confident errors and selects the configuration.</dd></div><div><dt>Scoring power</dt><dd>Adjusted offense + defense, in points per game. A component, not the complete model ranking.</dd></div><div><dt>Schedule strength</dt><dd>Mean current scoring power of completed FBS opponents. The probability feature additionally shrinks it toward zero.</dd></div><div><dt>Expected remaining wins</dt><dd>Sum of probabilities across modeled remaining games. Not the number of picks above 50%; unmodeled opponents are excluded.</dd></div><div><dt>Remaining-win distribution</dt><dd>Exact Poisson-binomial calculation assuming independent outcomes and fixed current strength. A scenario, not a calibrated season-record forecast.</dd></div><div><dt>Team data rates</dt><dd>Summed numerators divided by summed denominators. Other summaries are per available game. FBS reference values average team aggregates.</dd></div></dl></section>
<section id="coverage"><h2>Real games and explicit coverage</h2><p><a href="https://github.com/sportsdataverse/sportsdataverse-data" target="_blank" rel="noreferrer">SportsDataverse releases</a> provide ESPN-derived historical schedules, membership and box scores. The <a href="https://site.api.espn.com/apis/site/v2/sports/football/college-football/teams/194/schedule?season=2026" target="_blank" rel="noreferrer">public ESPN team-schedule endpoint</a> provides remaining fixtures. These are two delivery sources with the same ESPN upstream, not independent corroboration.</p><p>FBS-vs-FCS games appear in records and raw statistics, but not rating fits or model accuracy. Remaining FCS games have no fabricated probability. Unannounced postseason matchups are not invented.</p><p>Snapshot built {dateLabel(manifest.generatedAt)}. The site does not claim live updates; result cutoffs and schedule retrieval dates appear separately.</p><Table><TableHeader><TableRow>{['Season','FBS teams','All games','Model games','Last result'].map(x=><TableHead key={x}>{x}</TableHead>)}</TableRow></TableHeader><TableBody>{manifest.audit.map(a=><TableRow key={a.season}><TableCell>{a.season}</TableCell><TableCell>{a.teams}</TableCell><TableCell>{a.games}</TableCell><TableCell>{a.modelGames}</TableCell><TableCell>{dateLabel(a.lastGame)}</TableCell></TableRow>)}</TableBody></Table></section>
<section id="limits"><h2>What the data cannot tell us</h2><p>No injury, depth-chart, transfer, recruiting, weather, betting-line or play-by-play inputs are used. No EPA, true success rate, garbage-time or drive efficiency is claimed. Pass attempts plus rush attempts is a play-volume proxy with source sack conventions. Points include special-teams and defensive scoring.</p><p>Early samples and promoted teams are uncertain. Roster turnover can make priors stale. Richer features have not consistently beaten the original scoring benchmark. Source revisions affect retrospective results; missing data and zero-attempt rates remain missing.</p><p>Future forecasts hold strength fixed and use published rest intervals. The season distribution assumes independent games and ignores correlated injuries and future strength changes. Hypothetical matchups assume equal rest. Expected points and the complete model’s favorite can differ because the probability also considers efficiency.</p></section>
<section id="api"><h2>Reproduce it or build on it</h2><p><a href="https://github.com/heimweh17/saturday-lab" target="_blank" rel="noreferrer">Source</a> · <a href={asset('/data/manifest.json')} target="_blank">Manifest</a> · <a href={asset('/data/model.json')} target="_blank">Model report</a> · <a href={asset('/data/fixtures.json')} target="_blank">Schedules</a> · <a href={asset('/data/model-predictions.csv')} download>Predictions</a></p><div className="formula">GET /saturday-lab/data/2026.json<br/>GET /saturday-lab/data/box-2026.json<br/>GET /saturday-lab/data/model.json</div><p>GitHub Pages serves versioned JSON datasets. Matchups run in the browser using the same formula. Optional Next.js API routes remain in the source for server hosting. ESPN IDs identify teams; snapshot 99 is latest and other week keys are before-week states.</p><p>The repository documents ingestion, regeneration, the search protocol and tests. Team marks belong to their respective owners.</p></section></article></div></>}
